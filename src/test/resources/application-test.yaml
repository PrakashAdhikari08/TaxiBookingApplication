
spring:
  datasource:
    username: root
    password: root
    url: jdbc:tc:mysql://localhost:3306/taxidb
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
